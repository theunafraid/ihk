#!/usr/bin/sh

. @CMAKE_INSTALL_PREFIX@/bin/util.sh

# define WORKDIR
if [ "${AUTOTEST_HOME}" != "" ]; then
    . ${AUTOTEST_HOME}/bin/config.sh
fi

patch_and_build status_mckernel status_ihk || exit $?

sudo @CMAKE_INSTALL_PREFIX@/bin/ihk_os_shutdown08 -u $(id -u) -g $(id -g)
ret=$?

exit $ret
