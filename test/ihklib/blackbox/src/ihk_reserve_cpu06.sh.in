#!/usr/bin/sh

# insmod as root
sudo ${CMAKE_INSTALL_PREFIX}/bin/ihk_reserve_cpu06 -u $(id -u) -g $(id -g) -i

# try to reserve memory as non-root
${CMAKE_INSTALL_PREFIX}/bin/ihk_reserve_cpu06 -u $(id -u) -g $(id -g)

# rmmod as root
sudo ${CMAKE_INSTALL_PREFIX}/bin/ihk_reserve_cpu06 -u $(id -u) -g $(id -g) -r
